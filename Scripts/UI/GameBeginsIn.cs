/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GameBeginsIn : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (GameOverseer.GO.countdown >= 2) {
            GetComponent<Text>().text = "";
        } else if (GameOverseer.GO.countdown >= 1) {
            GetComponent<Text>().text = "Game starts in 2 seconds...";
        } else if (GameOverseer.GO.countdown >= 0) {
            GetComponent<Text>().text = "Game starts in 1 second...";
        }
    }

}*/
